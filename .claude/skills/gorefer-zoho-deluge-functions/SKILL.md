---
name: gorefer-zoho-deluge-functions
description: Use when creating, editing, or debugging any Deluge function for the GoRefer/PIFS Zoho estate (sweeps, gatekeepers, journey nudges, webhooks, schedule wrappers), when pasting a function into the Zoho console errors, when a function silently returns created=0/empty results, or when wiring a new function into the WA_Send_Queue pipeline. THIN POINTER — the canonical skill body moved to Zoho-Project (owner filing rule 2026-08-06: Zoho know-how lives in Zoho-Project, mirroring the whatsapp-flow-map move to Wati-Project, T-038).
---

# gorefer-zoho-deluge-functions — pointer (SSOT lives in Zoho-Project)

**This file is a pointer, not the skill.** The canonical, always-current body is:

```
D:\Abhay\VibeCoding\5Wealths\Zoho-Project\.claude\skills\gorefer-zoho-deluge-functions\SKILL.md
```

**STEP 0 (mandatory):** Read that file NOW and follow it exactly — registry, wiring map,
and every numbered standard (each bought with a live failure). Do not proceed from memory
of this pointer. Never duplicate the body here — a stale copy is worse than none
(same rule as the get-work-done hub pointer).

Moved 2026-08-06 per the owner's vendor-filing rule (CLAUDE.md §6b spirit): *reusable
vendor know-how → the platform folder*. The GoRefer-side integration contract docs
(`Zoho-GoRefer/`, `Wati-GoRefer/`) deliberately STAY in this repo — CI couples them to
the adapter code.

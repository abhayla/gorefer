# GoRefer — Sprint 1 runtime + dev dependencies (stack LOCKED, ADR-024).
# Multi-tenancy is single-schema tenant_id discriminator (Q-M1-1) — NO django-tenants.
Django>=5.1,<5.2
django-ninja>=1.6,<2.0
django-q2>=1.7              # background queue (ORM broker — no Redis)
psycopg2-binary>=2.9        # PostgreSQL driver (the sole supported engine — M10)
python-dotenv>=1.0
Pillow>=10.0                 # T-158: real image-decode check on Path-B evidence uploads

# Dev / CI
pytest>=8.0
pytest-django>=4.9,<4.13   # 4.13.0 (released ~2026-08-05) breaks django_db fixture setup (_pre_setup_ran_eagerly AttributeError); re-test before lifting
# DF-TESTDB-ISOLATION: gives each worker its OWN test DB (gorefer_test_gw0, _gw1, …),
# which is what makes concurrent runs safe — not merely faster.
pytest-xdist>=3.5
# Coverage (T-048 report-only → T-162 FLOOR). CI runs --cov-fail-under=86, five points
# below the 91% measured on main at 06cf2bc: existing PRs cannot trip it, but a real
# drop fails the build. The floor lives in .github/workflows/ci.yml, not here.
# Local `python -m pytest -q` without --cov flags is unchanged.
pytest-cov>=5.0,<8.0
ruff>=0.6

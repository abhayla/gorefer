# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Django
*.sqlite3
staticfiles/
/media/

# Env / secrets — NEVER commit
.env
.env.*
!.env.example

# Tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Node (frontend build tooling only; the COMPILED static/css/app.css + vendored
# static/js/htmx.min.js ARE committed so the app runs without Node at runtime).
node_modules/
package-lock.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
